interface:
  display_name: "Cadence Remote Batch"
  short_description: "Inspect OA, repair schematics, and run Spectre remotely."
  default_prompt: "Use $cadence-remote-batch to inspect a remote Cadence library, fix a schematic view, and run batch simulations."
